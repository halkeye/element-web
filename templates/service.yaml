apiVersion: v1
kind: Service
metadata:
  name: {{ include "element-io.fullname" . }}
  labels:
{{ include "element-io.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "element-io.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
