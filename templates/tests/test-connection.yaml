apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "element-io.fullname" . }}-test-connection"
  labels:
{{ include "element-io.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "element-io.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
